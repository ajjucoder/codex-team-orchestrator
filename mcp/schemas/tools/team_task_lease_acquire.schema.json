{
  "type": "object",
  "required": ["team_id", "task_id", "agent_id"],
  "properties": {
    "team_id": { "type": "string", "pattern": "^team_[A-Za-z0-9_-]+$" },
    "task_id": { "type": "string", "pattern": "^task_[A-Za-z0-9_-]+$" },
    "agent_id": { "type": "string", "pattern": "^agent_[A-Za-z0-9_-]+$" },
    "lease_ms": { "type": "integer", "minimum": 1 },
    "expected_lock_version": { "type": "integer", "minimum": 0 }
  },
  "additionalProperties": false
}

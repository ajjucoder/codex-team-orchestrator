{
  "type": "object",
  "required": ["team_id", "profile"],
  "properties": {
    "team_id": { "type": "string", "pattern": "^team_[A-Za-z0-9_-]+$" },
    "profile": { "type": "string", "enum": ["fast", "default", "deep"] }
  },
  "additionalProperties": false
}

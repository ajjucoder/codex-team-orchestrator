{
  "type": "object",
  "required": ["team_id"],
  "properties": {
    "team_id": { "type": "string", "pattern": "^team_[A-Za-z0-9_-]+$" },
    "recent_event_limit": { "type": "integer", "minimum": 1, "maximum": 300 },
    "evidence_limit": { "type": "integer", "minimum": 1, "maximum": 80 },
    "failure_limit": { "type": "integer", "minimum": 1, "maximum": 80 }
  },
  "additionalProperties": false
}

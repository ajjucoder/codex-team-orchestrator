{
  "type": "object",
  "required": ["team_id"],
  "properties": {
    "team_id": { "type": "string", "pattern": "^team_[A-Za-z0-9_-]+$" },
    "checkpoint_artifact_id": { "type": "string", "pattern": "^artifact_[A-Za-z0-9_-]+$|^checkpoint_[A-Za-z0-9_-]+$|^checkpoint_context$" },
    "checkpoint_version": { "type": "integer", "minimum": 1 }
  },
  "additionalProperties": false
}
